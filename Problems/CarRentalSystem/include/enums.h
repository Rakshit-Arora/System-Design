#pragma once

enum class Status { ACTIVE, INACTIVE };
enum class VehicleType { CAR, BIKE };
enum class PaymentMode { CASH, ONLINE };
enum class ReservationStatus { SCHEDULED, INPROGRESS, COMPLETED, CANCELLED };
enum class ReservationType { HOURLY, DAILY };
